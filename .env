# .env file

# SECURITY: This is your local development key.
SECRET_KEY='django-insecure-th(23hh66p&@)exm)ne%)vl6qm7$(4-sq_(e@crrivpj$(-mfm'

# This keeps DEBUG=True for your local environment
DEBUG=True

# Local database settings (from your current settings.py)
DB_NAME=resonate_db
DB_USER=root
DB_PASSWORD=speaker00
DB_HOST=localhost
DB_PORT=3306

# Only allow localhost for local development
ALLOWED_HOSTS=127.0.0.1,localhost

# Default email backend for local testing
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend